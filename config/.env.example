# Copy to config/.env for local overrides.
EIDOLON_DATA_SQLITE_PATH=~/eidolon/data/eidolon.sqlite3
EIDOLON_DATA_ECHO_SQL=false
# Required only by the narrow Companion Authority HTTP service.
EIDOLON_DATA_COMPANION_AUTHORITY_TOKEN=
